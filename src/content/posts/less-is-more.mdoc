---
title: 工具越少，写得越多
description: "给博客动了个小手术：去掉了 Keystatic CMS 和两百多 MB 的 node_modules。留下的只有一个更清醒的认知——写作工具的选择标准只有一个：它是否让你更愿意写。"
pubDatetime: 2026-07-28T04:00:00+08:00
tags:
  - 技术
  - 方法
draft: false
featured: false
---

这个博客刚开的时候，我装了一套完整的内容管理系统（Keystatic）——可视化编辑器、实时预览、媒体库管理，应有尽有。挺酷的。

但两个月后我发现一个尴尬的事实：我一篇文章都没用 Keystatic 写过。所有文章都是在 VS Code 里直接写的，写完 `git push` 就上线。

我决定把它卸了。

## 为什么装上去的，又拆了

当初装 Keystatic 的理由很充分：「万一哪天想用呢？」——但正是这个理由让我警惕。

在软件工程里有一种常见的错误估算：**你往往会高估一个复杂工具未来带来的收益，低估它现在引入的复杂度。**

Keystatic 带来了什么？

- 7 个 npm 包（包括 React、React-DOM，以及它们的类型定义）
- 135 个传递依赖
- 一套独立的配置文件（`keystatic.config.ts`）
- 一个需要条件启用的 Astro 集成
- 一条「唯一例外」的 CLAUDE.md 规则

而我想要的只是**写一篇 Markdown，然后发布**。

135 个包帮不了我写一个字——它们只是在给「可能有一天会用」的未来买单。

## 少一个工具，多一分清醒

去掉 Keystatic 之后，项目变得很干净：

- 构建从 3 步（启动本地 CMS → 写 → 构建）变成了 1 步（直接在编辑器写 → 构建）
- 没有 React，没有 JSX 配置，没有 SSR 路由的条件判断
- 代码库少了一份需要维护的配置

更重要的是，心理层面的变化：**少了那个「我还配了个 CMS」的自我安慰，只剩下一个朴素的事实——你要写。** 没有入口门槛，没有启动依赖，打开文件就能写。

## 什么时候该加工具？

不是所有工具都是负担，但每次加工具之前可以问自己三个问题：

1. **这个工具解决的是我现在的问题，还是我「担心以后会有」的问题？**
   — 后者等它真的发生了再说。

2. **引入这个工具后，我多了一个需要维护的东西，值不值？**
   — 依赖越多，升级越痛，兼容性越脆弱。

3. **如果不用这个工具，最坏的情况是什么？**
   — 最坏不过是写 Markdown 的时候没有实时预览。我能接受。

这三个问题之后，Keystatic 一个都没通过。

## 写才是目的

把工具卸掉的最后一个好处是：**你再也没有借口了。**

以前可以说「等我把 CMS 配好再开始认真写」。现在 VS Code 打开文件，光标闪烁，唯一缺少的就是内容。

这篇文章就是拆掉 Keystatic 之后的第一篇。从「写」开始，到「写」结束——这才是我想要的工作流。
